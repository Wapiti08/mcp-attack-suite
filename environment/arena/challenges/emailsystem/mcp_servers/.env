# ImageExtractorServer config (OpenAI-compatible multimodal)
#
# These values are intentionally scoped to this MCP server. If unset, the server
# will fall back to OPENAI_* (loaded by your environment-wide `.env`).

IMAGE_EXTRACT_OPENAI_BASE_URL=http://localhost:1234/v1/
IMAGE_EXTRACT_OPENAI_API_KEY=lm-studio
IMAGE_EXTRACT_OPENAI_MODEL=qwen3-vl-4b

